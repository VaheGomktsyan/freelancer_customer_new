"use client";
import { IUser } from "@/type/type";
import React, { useEffect } from "react";

export const Customer = ({user}:{user:IUser}) => {
    return (
        <div>
            <h3>Customer</h3>
        </div>
    );
};